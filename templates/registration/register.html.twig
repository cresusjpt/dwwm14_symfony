{% extends 'signbase.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{% for flashError in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}

	<h1>Register</h1>

	{# form()
					    form_row()
					    form_widget('name')
					    form_label('name') #}

	{{form(registrationForm) }}{% endblock %}
